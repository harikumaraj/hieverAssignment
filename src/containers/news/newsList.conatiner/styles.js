import {dpToPx} from "../../../utils"

export default styles = {
    container: {
        flex: 1
    },
    titleStyle: {
        marginHorizontal: dpToPx(6),
        paddingVertical: dpToPx(6),
        fontSize: dpToPx(8),
        fontWeight: "bold",
        color: "#000000",
        borderBottomWidth: dpToPx(0.2),
        borderBottomColor: "#aaaaaa"
    }
}